/**
 * Azure Functions entry point
 * Imports all function handlers to register them with the Functions runtime
 */

// Import functions to trigger their registration via app.http()
import './functions/get-commentary.js';
